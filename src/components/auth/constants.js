/**
 * Defines the text data on page.
 *
 * @constant
 */
const siteData = {
	siteNameText: 'health-e',
	siteDesc1Text:
		'Connect with friends around the world with great health topics',
};

/**
 * Defines the classes used in application.
 *
 * @constant
 */
const cssClasses = {
	AUTH_LAYOUT: 'l-authLayout',
	AUTH_LAYOUT_BRAND: 'l-authLayout__brand',
	AUTH_LAYOUT_BRAND_DESC: 'l-authLayout__brandDesc',
	AUTH_LAYOUT_BRAND_NAME: 'l-authLayout__brandName',
	AUTH_LAYOUT_IMG: 'l-authLayout__img',
	LEFT: 'left',
	RIGHT: 'right',
};

export { siteData, cssClasses };
